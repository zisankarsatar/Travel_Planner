import { Location } from "@/app/generated/prisma"

interface SortableItinerayProp{
    locations: Location[],
    tripId: string
}

export default function SortableItinerary({locations, tripId} : SortableItinerayProp){
    return<></>
}